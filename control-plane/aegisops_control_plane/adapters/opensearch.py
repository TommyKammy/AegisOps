from __future__ import annotations

from dataclasses import dataclass


@dataclass(frozen=True)
class OpenSearchSignalAdapter:
    """Placeholder for reviewed upstream analytic signal intake."""

    base_url: str
